<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>跳动的爱心</title>
  <style>
    :root { color-scheme: dark; }
    body{
      margin:0;
      height:100vh;
      display:grid;
      place-items:center;
      background:
        radial-gradient(1200px 800px at 20% 15%, rgba(255,80,150,.25), transparent 60%),
        radial-gradient(900px 700px at 80% 75%, rgba(120,120,255,.18), transparent 60%),
        #07070c;
      overflow:hidden;
      font-family: system-ui,-apple-system,"PingFang SC","Microsoft YaHei",sans-serif;
    }

    .stage{
      position:relative;
      width:min(320px, 70vw);
      aspect-ratio: 1 / 1;
      display:grid;
      place-items:center;
    }

    /* 柔和呼吸光晕 */
    .glow{
      position:absolute;
      inset:18%;
      border-radius:50%;
      background: radial-gradient(circle, rgba(255,60,120,.45), transparent 65%);
      filter: blur(18px);
      animation: breathe 1.6s ease-in-out infinite;
      opacity:.9;
    }

    svg{
      width:62%;
      height:auto;
      overflow:visible;
      transform-origin: 50% 65%;
      animation: beat 1.05s cubic-bezier(.2,.9,.25,1) infinite;
      filter:
        drop-shadow(0 16px 22px rgba(255, 40, 110, .22))
        drop-shadow(0 6px 10px rgba(255, 40, 110, .20));
    }

    /* 心形渐变+高光 */
    .heart-fill{
      fill: url(#g);
      stroke: rgba(255,255,255,.22);
      stroke-width: 2;
    }
    .shine{
      fill: rgba(255,255,255,.25);
      filter: blur(0.2px);
      opacity:.75;
      transform: translate(0, 0);
    }

    @keyframes beat{
      0%   { transform: scale(1); }
      14%  { transform: scale(1.12); }
      28%  { transform: scale(1.02); }
      42%  { transform: scale(1.18); }
      60%  { transform: scale(1.05); }
      100% { transform: scale(1); }
    }
    @keyframes breathe{
      0%,100%{ transform: scale(.95); opacity:.75; }
      50%    { transform: scale(1.05); opacity:1; }
    }

    .tip{
      position:fixed;
      bottom:18px;
      color:rgba(255,255,255,.7);
      font-size:12px;
      letter-spacing:.3px;
      user-select:none;
    }
  </style>
</head>
<body>
  <div class="stage">
    <div class="glow" aria-hidden="true"></div>

    <svg viewBox="0 0 200 200" role="img" aria-label="跳动的爱心">
      <defs>
        <linearGradient id="g" x1="40" y1="30" x2="170" y2="180" gradientUnits="userSpaceOnUse">
          <stop offset="0" stop-color="#ff77b7"/>
          <stop offset="0.5" stop-color="#ff2d6d"/>
          <stop offset="1" stop-color="#c7004c"/>
        </linearGradient>
      </defs>

      <!-- 更圆润的心形路径 -->
      <path class="heart-fill"
        d="M100 170
           C 55 140, 22 112, 22 78
           C 22 52, 40 36, 60 36
           C 76 36, 90 45, 100 58
           C 110 45, 124 36, 140 36
           C 160 36, 178 52, 178 78
           C 178 112, 145 140, 100 170 Z" />

      <!-- 高光（让它更立体） -->
      <path class="shine"
        d="M60 62
           C 52 72, 52 88, 62 98
           C 70 106, 82 108, 92 104
           C 78 94, 72 78, 76 64
           C 70 60, 64 60, 60 62 Z"/>
    </svg>
  </div>

  <div class="tip">（已换成更顺滑的发光爱心）</div>
</body>
</html>
